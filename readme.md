A Laravel package for scaffolding Laravel files using OpenAI's GPT-4

After installing run:

```
php artisan vendor:publish --provider="JamesBlackwell\ArtisanAI\ArtisanAIServiceProvider" --tag=config
```
